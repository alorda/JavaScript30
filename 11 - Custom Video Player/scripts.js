// Get elements
const player = document.querySelector('.player')
const video = player.querySelector('.viewer')
const progress = querySelector('progress')
const progressBar = querySelector('')
const progress = .querySelector('')
const progress = .querySelector('')